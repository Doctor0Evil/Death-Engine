[package]
name = "bci_ema_smoothing"
version = "0.1.0"
edition = "2021"

[lib]
name = "hpc_bci_ema"
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
once_cell = "1.19"
parking_lot = "0.12"
